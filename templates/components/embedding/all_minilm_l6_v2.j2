{% block all_minilm_l6_v2 %}
        try:
            response = requests.post(Config.TORCHSERVE_URL, json=data, headers={"Content-Type": "application/json"})
            response.raise_for_status()
            result = response.json()
        except Exception as e:
            print(f"MiniLM error: {e}")
            result = None
{% endblock %}